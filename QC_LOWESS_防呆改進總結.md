# QC_LOWESS_v2.py 防呆措施改進總結

## 改進日期
2025-11-16

## 改進目標
為 QC_LOWESS_v2.py 腳本添加完善的防呆機制和錯誤處理，確保腳本能夠：
- 正確處理各種異常輸入情況
- 提供清晰的錯誤訊息和建議
- 避免在錯誤數據上執行計算
- 確保輸出文件的完整性

---

## 一、數據載入階段（load_and_process_data 函數）

### 1. 文件相關防呆

#### 防呆1：文件存在性檢查
- **檢查內容**：驗證輸入文件是否存在
- **錯誤訊息**：`❌ 錯誤：找不到檔案 '{file_path}'`
- **處理方式**：返回 None，終止執行

#### 防呆2：文件格式檢查
- **檢查內容**：驗證文件是否為 Excel 格式（.xlsx 或 .xls）
- **錯誤訊息**：`❌ 錯誤：輸入檔案必須是 Excel 格式 (.xlsx 或 .xls)`
- **處理方式**：返回 None，終止執行

#### 防呆3：文件大小檢查
- **檢查內容**：
  - 檢查文件大小是否為 0 bytes（空文件）
  - 檢查文件大小是否異常小（< 1KB）
- **錯誤訊息**：
  - `❌ 錯誤：檔案大小為 0 bytes，可能是空檔案`
  - `⚠️  警告：檔案大小僅 {size} bytes，可能不是有效的 Excel 檔案`
- **處理方式**：空文件終止執行，小文件顯示警告但繼續

#### 防呆4：Excel 文件有效性檢查
- **檢查內容**：嘗試讀取 Excel 文件，捕獲損壞或格式錯誤
- **錯誤訊息**：`❌ 錯誤：無法讀取 Excel 檔案，可能已損壞或格式不正確`
- **處理方式**：返回 None，終止執行

---

### 2. 工作表相關防呆

#### 防呆5：必要工作表檢查
- **檢查內容**：驗證 'ISTD_Correction' 和 'SampleInfo' 工作表是否存在
- **錯誤訊息**：`❌ 錯誤：輸入檔案缺少必要的工作表: {missing_sheets}`
- **處理方式**：返回 None，終止執行
- **提示訊息**：`提示：QC-LOWESS 校正需要先執行 ISTD_Correction`

---

### 3. SampleInfo 相關防呆

#### 防呆6：SampleInfo 完整性檢查
- **檢查內容**：
  - 驗證 SampleInfo 工作表不為空
  - 驗證必要欄位存在：'Sample_Name', 'Sample_Type', 'Injection_Order'
- **錯誤訊息**：
  - `❌ 錯誤：'SampleInfo' 工作表為空`
  - `❌ 錯誤：'SampleInfo' 缺少必要欄位: {missing_cols}`
- **處理方式**：返回 None，終止執行

#### 防呆7：樣本名稱重複檢查
- **檢查內容**：檢測 Sample_Name 中是否有重複值
- **警告訊息**：`⚠️  警告：'SampleInfo' 中發現重複的樣本名稱`
- **處理方式**：顯示警告，列出重複樣本，繼續執行

#### 防呆8：樣本類型檢查
- **檢查內容**：
  - 統計並顯示所有樣本類型
  - 檢查 QC 樣本數量（Sample_Type 包含 'QC'）
- **錯誤訊息**：`❌ 錯誤：未找到 QC 樣本（Sample_Type 中無 'QC' 字樣）`
- **警告訊息**：`⚠️  警告：QC 樣本數量不足 ({qc_count} < 5)`
- **提示訊息**：`提示：QC-LOWESS 校正需要至少 5 個 QC 樣本`
- **處理方式**：
  - 無 QC 樣本：返回 None，終止執行
  - QC 樣本 < 5：顯示警告，繼續執行

#### 防呆9：Injection_Order 有效性檢查
- **檢查內容**：
  - 檢查是否有樣本缺少 Injection_Order
  - 嘗試將 Injection_Order 轉換為數值類型
  - 統計轉換失敗的樣本數
- **警告訊息**：
  - `⚠️  警告：發現 {count} 個樣本缺少 Injection_Order`
  - `⚠️  警告：{count} 個樣本的 Injection_Order 無法轉換為數值`
- **處理方式**：顯示警告，列出問題樣本（最多顯示 5 個），繼續執行

---

### 4. ISTD_Correction 數據相關防呆

#### 防呆10：ISTD_Correction 基本檢查
- **檢查內容**：
  - 驗證 ISTD_Correction 工作表不為空
  - 驗證 'FeatureID' 欄位存在
- **錯誤訊息**：
  - `❌ 錯誤：'ISTD_Correction' 工作表為空`
  - `❌ 錯誤：'ISTD_Correction' 缺少 'FeatureID' 欄位`
- **處理方式**：返回 None，終止執行

#### 防呆11：FeatureID 重複檢查
- **檢查內容**：檢測 FeatureID 中是否有重複值
- **警告訊息**：`⚠️  警告：'ISTD_Correction' 中發現重複的 FeatureID`
- **處理方式**：顯示警告，列出重複的 FeatureID（最多顯示 5 個），繼續執行

#### 防呆12：樣本欄位檢查
- **檢查內容**：
  - 識別樣本欄位（排除 'FeatureID', 'RT', 'ISTD', 'ISTD_RT', 'RT_Difference', 'ISTD_Median', 'QC_CV%'）
  - 驗證至少存在一個樣本欄位
- **錯誤訊息**：`❌ 錯誤：'ISTD_Correction' 中沒有樣本欄位`
- **處理方式**：返回 None，終止執行

#### 防呆13：樣本名稱匹配檢查
- **檢查內容**：
  - 檢查 SampleInfo 中的樣本是否在 ISTD_Correction 中存在
  - 檢查 ISTD_Correction 中的樣本是否在 SampleInfo 中存在
- **警告訊息**：
  - `⚠️  警告：以下樣本在 SampleInfo 中有記錄，但在 ISTD_Correction 中找不到`
  - `⚠️  警告：以下樣本在 ISTD_Correction 中有數據，但在 SampleInfo 中找不到`
- **處理方式**：顯示警告，列出不匹配的樣本（最多顯示 5 個），繼續執行

#### 防呆14：數值類型轉換
- **檢查內容**：強制將所有樣本欄位轉換為數值類型（使用 pd.to_numeric）
- **處理方式**：自動轉換，無法轉換的值變為 NaN

#### 防呆15：全為 NaN 或 0 的列檢查
- **檢查內容**：檢查是否有樣本的所有數值都是 0 或 NaN
- **警告訊息**：`⚠️  警告：以下樣本的所有數值都是 0 或 NaN`
- **處理方式**：顯示警告，列出問題樣本（最多顯示 5 個），繼續執行

#### 防呆16：數值範圍檢查
- **檢查內容**：
  - 檢查負值
  - 檢查極端高值（> 1e12）
- **警告訊息**：
  - `⚠️  警告：樣本 '{col}' 包含 {count} 個負值`
  - `⚠️  警告：樣本 '{col}' 包含 {count} 個極端高值 (> 1e12)`
- **處理方式**：
  - 負值：自動將負值裁剪為 0
  - 極端高值：顯示警告，繼續執行

---

## 二、LOWESS 處理階段（perform_lowess_normalization 函數）

### 防呆1：輸入數據有效性檢查
- **檢查內容**：驗證 istd_df 和 sample_info_df 不為 None 且不為空
- **錯誤訊息**：
  - `❌ 錯誤：ISTD_Correction 數據為空`
  - `❌ 錯誤：SampleInfo 數據為空`
- **處理方式**：返回 None，終止執行

### 防呆2：必要欄位檢查
- **檢查內容**：驗證必要欄位存在
- **錯誤訊息**：
  - `❌ 錯誤：ISTD_Correction 缺少 'FeatureID' 欄位`
  - `❌ 錯誤：SampleInfo 缺少必要欄位`
- **處理方式**：返回 None，終止執行

### 防呆3：樣本欄位檢查
- **檢查內容**：驗證至少存在一個樣本欄位
- **錯誤訊息**：`❌ 錯誤：找不到任何樣本欄位`
- **處理方式**：返回 None，終止執行

### 防呆4：QC 樣本識別
- **檢查內容**：從 SampleInfo 或樣本名稱中識別 QC 樣本
- **處理方式**：優先使用 Sample_Type 欄位，否則根據名稱判斷

### 防呆5：QC 樣本數量檢查
- **檢查內容**：驗證 QC 樣本數量 >= 5
- **錯誤訊息**：`❌ 錯誤：QC 樣本不足 ({count} < 5)，無法進行校正`
- **提示訊息**：`提示：LOWESS 校正需要至少 5 個 QC 樣本以確保擬合準確性`
- **處理方式**：返回 None，終止執行

---

## 三、文件保存階段（save_results_to_excel 函數）

### 防呆1：輸入數據有效性檢查
- **檢查內容**：驗證 istd_df、lowess_df 和 sample_info_df 不為 None 且不為空
- **錯誤訊息**：
  - `❌ 錯誤：ISTD_Correction 數據為空，無法保存`
  - `❌ 錯誤：LOWESS 校正結果為空，無法保存`
  - `❌ 錯誤：SampleInfo 數據為空，無法保存`
- **處理方式**：返回 False

### 防呆2：輸出路徑有效性檢查
- **檢查內容**：檢查輸出目錄是否存在，若不存在則嘗試創建
- **警告訊息**：`⚠️  警告：輸出目錄不存在，嘗試創建`
- **錯誤訊息**：`❌ 錯誤：無法創建輸出目錄`
- **處理方式**：
  - 成功創建：繼續執行
  - 創建失敗：返回 False

### 防呆3：輸出目錄可寫性檢查
- **檢查內容**：驗證對輸出目錄有寫入權限
- **錯誤訊息**：`❌ 錯誤：沒有寫入權限到目錄`
- **處理方式**：返回 False

### 防呆4：輸入文件有效性檢查
- **檢查內容**：驗證輸入文件存在（用於複製格式）
- **錯誤訊息**：`❌ 錯誤：找不到輸入檔案`
- **處理方式**：返回 False

### 防呆5：文件保存檢查
- **檢查內容**：捕獲文件保存過程中的錯誤
- **錯誤訊息**：
  - `❌ 錯誤：無法保存檔案，可能檔案已被其他程式開啟`（PermissionError）
  - `❌ 錯誤：保存檔案時發生錯誤`（其他異常）
- **處理方式**：
  - 關閉 workbook
  - 返回 False

### 防呆6：文件保存驗證
- **檢查內容**：
  - 驗證輸出文件存在
  - 檢查文件大小不為 0
  - 檢查文件大小是否異常小（< 1KB）
- **錯誤訊息**：
  - `❌ 錯誤：檔案保存失敗，找不到輸出檔案`
  - `❌ 錯誤：輸出檔案大小為 0 bytes`
- **警告訊息**：`⚠️  警告：輸出檔案大小異常小 ({size} bytes)`
- **處理方式**：顯示文件大小信息

---

## 四、PCA 分析階段（perform_pca_analysis 函數）

### 防呆1：輸入數據有效性檢查
- **檢查內容**：驗證所有輸入數據不為 None 且不為空
- **錯誤訊息**：
  - `❌ 錯誤：ISTD_Correction 數據為空，無法進行 PCA 分析`
  - `❌ 錯誤：LOWESS 校正結果為空，無法進行 PCA 分析`
  - `❌ 錯誤：SampleInfo 數據為空，無法進行 PCA 分析`
  - `❌ 錯誤：樣本欄位為空，無法進行 PCA 分析`
- **處理方式**：直接返回，跳過 PCA 分析

### 防呆2：輸出目錄設置和檢查
- **檢查內容**：
  - 創建輸出目錄
  - 檢查目錄可寫性
- **錯誤訊息**：
  - `❌ 錯誤：無法創建輸出目錄`
  - `❌ 錯誤：沒有寫入權限到目錄`
- **處理方式**：直接返回，跳過 PCA 分析

### 防呆3：圖表保存檢查
- **檢查內容**：
  - 捕獲保存過程中的異常
  - 驗證文件存在
  - 檢查文件大小
- **警告訊息**：
  - `⚠️  警告：PCA 圖表保存失敗，找不到輸出檔案`
  - `⚠️  警告：PCA 圖表大小為 0 bytes`
  - `⚠️  警告：保存 PCA 圖表時發生錯誤`
- **處理方式**：顯示警告，關閉 matplotlib

---

## 五、P 值分佈圖階段（plot_pvalue_distribution 函數）

### 防呆1：輸入數據檢查
- **檢查內容**：
  - 驗證 cv_results_df 不為 None 且不為空
  - 驗證 'Variance_Test_pvalue' 欄位存在
- **警告訊息**：
  - `⚠️  警告：CV 結果數據為空，無法繪製 p 值分佈圖`
  - `⚠️  警告：找不到 Variance_Test_pvalue 欄位，無法繪製 p 值分佈圖`
- **處理方式**：直接返回，跳過繪圖

### 防呆2：輸出目錄檢查
- **檢查內容**：
  - 創建輸出目錄
  - 檢查目錄可寫性
- **警告訊息**：
  - `⚠️  警告：無法創建輸出目錄`
  - `⚠️  警告：沒有寫入權限到目錄`
- **處理方式**：直接返回，跳過繪圖

### 防呆3：圖表保存檢查
- **檢查內容**：
  - 捕獲保存過程中的異常
  - 驗證文件存在
  - 檢查文件大小
- **警告訊息**：
  - `⚠️  警告：P 值分佈圖保存失敗，找不到輸出檔案`
  - `⚠️  警告：P 值分佈圖大小為 0 bytes`
  - `⚠️  警告：保存 P 值分佈圖時發生錯誤`
- **處理方式**：顯示警告，關閉 matplotlib

---

## 六、改進效果總結

### 1. 錯誤預防
- **文件層級**：確保輸入文件存在、格式正確、可讀取
- **數據層級**：驗證必要欄位存在、數據類型正確、數值範圍合理
- **邏輯層級**：確保 QC 樣本充足、樣本名稱匹配、注射順序有效

### 2. 錯誤訊息清晰度
- **分類明確**：使用 ❌（錯誤）和 ⚠️（警告）區分嚴重程度
- **訊息詳細**：提供具體的錯誤原因和受影響的數據
- **建議友好**：給出解決問題的提示和建議

### 3. 處理策略分層
- **致命錯誤**：立即終止執行，返回 None 或 False
- **警告信息**：顯示警告但繼續執行，讓用戶決定
- **自動修復**：對於可修復的問題（如負值），自動處理並告知用戶

### 4. 輸出驗證
- **文件存在性**：確保輸出文件確實被創建
- **文件完整性**：檢查文件大小，避免空文件或異常小文件
- **權限檢查**：提前驗證寫入權限，避免執行後才發現無法保存

---

## 七、使用建議

### 對於數據準備者
1. 確保輸入 Excel 文件包含完整的工作表（ISTD_Correction 和 SampleInfo）
2. 確保 SampleInfo 包含必要欄位：Sample_Name、Sample_Type、Injection_Order
3. 確保至少有 5 個 QC 樣本
4. 檢查樣本名稱在不同工作表中的一致性

### 對於腳本使用者
1. 仔細閱讀錯誤訊息和警告
2. 根據提示修正數據問題
3. 對於警告信息，評估是否需要修正數據
4. 確保輸出目錄有足夠的寫入權限

### 對於開發者
1. 所有防呆檢查都有明確的註釋（如 `===== 防呆X: 描述 =====`）
2. 錯誤處理遵循一致的模式
3. 可以根據需要調整閾值（如 QC 樣本數量、文件大小等）

---

## 八、與 ISTD_Correction_v2.py 的防呆對比

### 相同的防呆措施
- 文件存在性、格式、大小檢查
- 工作表存在性和完整性檢查
- 樣本名稱重複和匹配檢查
- 數值類型轉換和範圍檢查
- 輸出目錄和權限檢查
- 文件保存驗證

### QC_LOWESS 特有的防呆
- **QC 樣本數量檢查**：LOWESS 需要足夠的 QC 樣本（至少 5 個）
- **Injection_Order 檢查**：LOWESS 依賴注射順序進行趨勢擬合
- **ISTD_Correction 工作表檢查**：QC-LOWESS 以 ISTD_Correction 的結果為輸入
- **PCA 分析輸入檢查**：確保有足夠的樣本和特徵進行 PCA

### ISTD_Correction 特有的防呆
- **ISTD 標記檢查**：識別紅色標記的 ISTD 特徵
- **RT 範圍檢查**：保留時間的合理性驗證
- **ISTD-特徵配對檢查**：確保每個特徵有對應的 ISTD

---

## 九、測試建議

### 正常情況測試
- 使用完整、正確的數據文件
- 驗證所有功能正常運行
- 檢查輸出文件和圖表

### 異常情況測試
1. **缺少文件**：輸入不存在的文件路徑
2. **錯誤格式**：使用 .csv 或其他非 Excel 格式
3. **空文件**：使用 0 bytes 的文件
4. **缺少工作表**：刪除必要工作表
5. **缺少欄位**：刪除必要欄位
6. **QC 樣本不足**：只保留 < 5 個 QC 樣本
7. **樣本名稱不匹配**：修改部分樣本名稱
8. **數值異常**：引入負值、NaN、極端值
9. **權限問題**：輸出到只讀目錄

---

## 十、未來改進方向

1. **批次處理支持**：添加對多個文件的批次處理能力
2. **配置文件**：允許用戶自定義閾值（如 QC 樣本數量、文件大小等）
3. **日誌記錄**：將所有警告和錯誤記錄到日誌文件
4. **數據質量報告**：生成詳細的數據質量報告
5. **自動修復功能**：對於某些問題提供自動修復選項
6. **進度條**：為長時間運行的操作添加進度條
7. **並行處理**：優化大數據集的處理速度

---

**版本**：v2.0
**作者**：Claude
**最後更新**：2025-11-16
