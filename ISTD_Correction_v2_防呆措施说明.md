# ISTD_Correction_v2.py 防呆措施完善说明

## 改进概述

本次更新为 `ISTD_Correction_v2.py` 脚本添加了 **23 个防呆检查点**，大幅提升了脚本的稳定性和用户友好性。

---

## 详细防呆措施清单

### 一、输入文件验证（防呆 1-4）

#### **防呆1: 文件存在性检查**
- **位置**: `load_and_process_data()` 第 38 行
- **功能**: 检查输入文件是否存在
- **错误提示**: `"錯誤：找不到檔案 '{file_path}'"`

#### **防呆2: 文件格式检查**
- **位置**: 第 43 行
- **功能**: 确保输入文件为 Excel 格式（.xlsx 或 .xls）
- **错误提示**: `"錯誤：輸入檔案必須是Excel格式"`

#### **防呆3: 文件大小检查**
- **位置**: 第 48 行
- **功能**:
  - 检测空文件（0 bytes）
  - 警告过小的文件（< 1KB）
- **错误提示**:
  - `"錯誤：檔案大小為 0 bytes，可能是空檔案"`
  - `"警告：檔案大小僅 X bytes，可能不是有效的 Excel 檔案"`

#### **防呆4: Excel 文件有效性检查**
- **位置**: 第 58 行
- **功能**: 尝试读取 Excel 文件，捕获损坏文件
- **错误提示**: `"錯誤：無法讀取 Excel 檔案，可能已損壞或格式不正確"`

---

### 二、工作表完整性验证（防呆 5-8）

#### **防呆5: 必要工作表检查**
- **位置**: 第 70 行
- **功能**: 检查是否存在 `RawIntensity` 和 `SampleInfo` 工作表
- **错误提示**:
  - `"錯誤：輸入檔案缺少必要的工作表: ..."`
  - 列出找到的所有工作表

#### **防呆6: SampleInfo 完整性检查**
- **位置**: 第 78 行
- **功能**:
  - 检查 SampleInfo 是否为空
  - 检查必要欄位（Sample_Name, Sample_Type）
- **错误提示**:
  - `"錯誤：'SampleInfo' 工作表為空"`
  - `"錯誤：'SampleInfo' 缺少必要欄位: ..."`

#### **防呆7: 样本名称重复检查**
- **位置**: 第 93 行
- **功能**: 检测 SampleInfo 中的重复样本名称
- **警告提示**: 列出所有重复的样本名称

#### **防呆8: 样本类型检查**
- **位置**: 第 101 行
- **功能**:
  - 识别并显示所有样本类型
  - 检查是否存在 QC 样本
- **警告提示**: `"警告：未找到 QC 樣本"`

---

### 三、RawIntensity 数据验证（防呆 9-13, 16）

#### **防呆9: RawIntensity 基本检查**
- **位置**: 第 127 行
- **功能**:
  - 检查 RawIntensity 是否为空
  - 检查 FeatureID 欄位是否存在
- **错误提示**:
  - `"錯誤：'RawIntensity' 工作表為空"`
  - `"錯誤：'RawIntensity' 缺少 'FeatureID' 欄位"`

#### **防呆10: FeatureID 重复检查**
- **位置**: 第 137 行
- **功能**: 检测重复的 FeatureID
- **警告提示**: 列出重复的 FeatureID（最多显示 5 个）

#### **防呆11: 样本列检查**
- **位置**: 第 148 行
- **功能**: 确保至少有一个样本欄位
- **错误提示**: `"錯誤：'RawIntensity' 中沒有樣本欄位"`

#### **防呆12: 样本名称匹配检查**
- **位置**: 第 160 行
- **功能**: 交叉验证 SampleInfo 和 RawIntensity 中的样本名称
- **警告提示**:
  - 列出在 SampleInfo 中但不在 RawIntensity 中的样本
  - 列出在 RawIntensity 中但不在 SampleInfo 中的样本

#### **防呆13: 全为 NaN 或 0 的列检查**
- **位置**: 第 185 行
- **功能**: 检测所有值都是 0 或 NaN 的样本欄位
- **警告提示**: `"警告：樣本 'X' 的所有數值都是 0 或 NaN"`

#### **防呆16: 数值范围检查**
- **位置**: 第 194 行
- **功能**:
  - 检测并修正负值（设为 0）
  - 检测极端高值（> 1e15）
- **警告提示**:
  - `"警告：樣本 'X' 有 N 個負值，已設為 0"`
  - `"警告：樣本 'X' 有極端高值 (Y)，請檢查數據是否正確"`

---

### 四、ISTD 识别与验证（防呆 14-15）

#### **防呆14: ISTD 识别验证**
- **位置**: 第 232 行
- **功能**:
  - 检查是否找到 ISTD（红色标记的 FeatureID）
  - 警告 ISTD 数量过少（< 3 个）
  - 验证标记的 ISTD 是否在 RawIntensity 中存在
- **错误提示**:
  - `"錯誤：未找到任何 ISTD"`
  - `"警告：ISTD 數量較少 (X 個)，建議至少使用 3 個以上的 ISTD"`

#### **防呆15: ISTD 强度验证**
- **位置**: 第 253 行
- **功能**:
  - 检查 ISTD 的强度是否全为 0 或 NaN
  - 检查有效值比例
  - 计算并警告 CV% 过高的 ISTD（> 30%）
- **错误提示**:
  - `"錯誤：ISTD 'X' 的所有樣本強度都是 0 或 NaN"`
  - `"警告：ISTD 'X' 有效值比例較低 (M/N)"`
  - `"警告：ISTD 'X' 的 CV% 較高 (Y%)，可能影響校正品質"`

---

### 五、校正计算验证（防呆 19-21）

#### **防呆19: 基本输入验证**
- **位置**: `calculate_corrected_ratios()` 第 480 行
- **功能**: 验证输入数据和样本信息不为空
- **错误提示**:
  - `"錯誤：輸入數據為空"`
  - `"錯誤：樣本資訊為空"`

#### **防呆20: ISTD 信号检查**
- **位置**: 第 491 行
- **功能**:
  - 检查是否找到 ISTD 信号
  - 检查是否有代谢物信号
- **错误提示**:
  - `"錯誤：未找到 ISTD 信號"`
  - `"錯誤：未找到代謝物信號（所有 FeatureID 都被標記為 ISTD）"`

#### **防呆21: 权重参数验证**
- **位置**: `find_best_istd_for_analyte()` 第 338 行
- **功能**:
  - 检查权重是否为负
  - 检查权重总和是否为 1.0
- **错误提示**:
  - `"錯誤：權重不能為負值"`
  - `"錯誤：權重總和 = X，必須等於 1.0"`

---

### 六、结果保存验证（防呆 17-18, 22）

#### **防呆17: 输出目录权限检查**
- **位置**: `main()` 第 1589 行
- **功能**: 测试输出目录的写入权限
- **错误提示**: `"錯誤：無法寫入 output 目錄"`

#### **防呆18: 输出文件检查**
- **位置**: 第 1602 行
- **功能**: 警告输出文件已存在将被覆盖
- **警告提示**: `"警告：輸出檔案已存在，將被覆蓋"`

#### **防呆22: 结果数据验证**
- **位置**: `save_results_to_excel()` 第 1398 行
- **功能**:
  - 验证结果数据不为空
  - 检查必要欄位是否存在
  - 显示统计信息
- **错误提示**:
  - `"錯誤：校正結果為空，無法儲存"`
  - `"錯誤：結果缺少必要欄位: ..."`

---

### 七、统计分析参数验证（防呆 23）

#### **防呆23: alpha 参数验证**
- **位置**: `calculate_hotelling_t2_outliers()` 第 797 行
- **功能**: 确保显著水平 alpha 在 (0, 1) 范围内
- **错误提示**: `"錯誤：alpha 必須在 (0, 1) 範圍內，當前值: X"`

---

## 改进效果总结

### 1. **提升数据质量保障**
- 多层次验证确保输入数据的完整性和有效性
- 及早发现数据问题，避免后续计算错误

### 2. **增强用户体验**
- 清晰的错误提示和警告信息
- 详细的统计信息输出
- 友好的中文错误消息

### 3. **提高脚本稳定性**
- 参数范围验证防止异常输入
- 文件操作前的权限检查
- 数值范围检查防止计算溢出

### 4. **便于问题诊断**
- 每个错误都有明确的来源和原因
- 显示相关的统计信息帮助用户理解问题
- 建议性的解决方案

---

## 使用建议

1. **首次使用前**
   - 确保输入文件符合格式要求
   - 在 RawIntensity 工作表中将 ISTD 的 FeatureID 标记为红色

2. **遇到错误时**
   - 仔细阅读错误提示信息
   - 根据提示检查相应的数据或文件
   - 如需帮助，提供完整的错误信息

3. **数据准备**
   - 确保 SampleInfo 包含 Sample_Name 和 Sample_Type 欄位
   - 确保至少有 3 个 QC 样本用于统计分析
   - 确保至少有 3 个 ISTD 用于校正

---

## 技术细节

### 错误处理策略
- **严重错误**: 返回 None 并终止当前步骤
- **警告**: 显示警告信息但继续执行
- **自动修正**: 修正数据并记录修正内容（如负值设为 0）

### 日志级别
- ❌ **错误**: 导致流程终止的问题
- ⚠️ **警告**: 可能影响结果但不终止流程的问题
- ✓ **信息**: 正常的处理过程说明

---

## 版本信息

- **脚本名称**: ISTD_Correction_v2.py
- **改进版本**: 2025-11-16
- **防呆措施数量**: 23 个
- **主要改进类别**:
  - 输入验证（4 个）
  - 数据完整性（9 个）
  - ISTD 验证（2 个）
  - 计算参数（3 个）
  - 输出保护（3 个）
  - 统计分析（1 个）
  - 其他（1 个）

---

## 附录：快速故障排除

| 错误类型 | 可能原因 | 解决方案 |
|---------|---------|---------|
| 文件不存在 | 文件路径错误或文件已删除 | 检查文件路径是否正确 |
| 文件格式错误 | 文件不是 Excel 格式 | 确保文件为 .xlsx 或 .xls |
| 缺少工作表 | Excel 文件不包含必要工作表 | 添加 RawIntensity 和 SampleInfo 工作表 |
| 未找到 ISTD | 未将 ISTD 标记为红色 | 在 RawIntensity 中将 ISTD 的 FeatureID 设为红色字体 |
| 未找到 QC | SampleInfo 中没有 QC 样本 | 在 Sample_Type 欄位中标记 QC 样本 |
| 权重错误 | 权重参数设置不正确 | 确保所有权重为正且总和为 1.0 |
| 输出目录无权限 | 目录被保护或不存在 | 检查目录权限或手动创建 output 目录 |

---

**最后更新**: 2025-11-16
