> [!IMPORTANT]
> Archived historical plan. It may describe pre-refactor Step 3/Step 4 behavior that is no longer current.
> Current workflow sources: `README.md`, `docs/TESTING.md`,
> `docs/plans/2026-04-23-dnp-workflow-responsibility-spec.md`, and
> `docs/plans/2026-04-23-pqn-reference-selection-rules.md`.

# Step 3 Figure Renumbering Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Renumber the new Step 3 `QC Batch Scaling` figures so the three emitted diagnostics use a consecutive `Fig1 / Fig2 / Fig3` sequence.

**Architecture:** Keep the change isolated to `src/metabolomics/processors/qc_batch_scaling.py` and the tests that verify Step 3 diagnostics output. Do not change any plotting content or touch the legacy `batch_effect.py` naming scheme.

**Tech Stack:** Python, pathlib, pytest

---

### Task 1: Lock the new Step 3 figure names with failing tests

**Files:**
- Modify: `C:/Users/user/Desktop/Data_Normalization_project_v2/tests/unit/test_qc_batch_scaling.py`

**Step 1: Write the failing tests**

- Update the diagnostics-output regression so it expects:
  - `Fig1_PCA_by_batch_<timestamp>.png`
  - `Fig2_PCA_by_sample_type_<timestamp>.png`
  - `Fig3_Residual_Analysis_<timestamp>.png`
- Remove any remaining expectation for `Fig6_Residual_Analysis_<timestamp>.png`

**Step 2: Run test to verify it fails**

Run:
`pytest tests/unit/test_qc_batch_scaling.py -q`

Expected:
FAIL because Step 3 still emits `Fig2/4/6`.

**Step 3: Write minimal implementation**

- Adjust the Step 3 output filename prefixes in `qc_batch_scaling.py`

**Step 4: Run test to verify it passes**

Run:
`pytest tests/unit/test_qc_batch_scaling.py -q`

Expected:
PASS

### Task 2: Renumber the Step 3 diagnostics filenames

**Files:**
- Modify: `C:/Users/user/Desktop/Data_Normalization_project_v2/src/metabolomics/processors/qc_batch_scaling.py`
- Test: `C:/Users/user/Desktop/Data_Normalization_project_v2/tests/unit/test_qc_batch_scaling.py`

**Step 1: Change the three filename prefixes**

Update:

```python
batch_plot_path = os.path.join(plots_dir, f"Fig1_PCA_by_batch_{timestamp}.png")
sample_type_plot_path = os.path.join(plots_dir, f"Fig2_PCA_by_sample_type_{timestamp}.png")
residual_plot_path = os.path.join(plots_dir, f"Fig3_Residual_Analysis_{timestamp}.png")
```

Do not change:

- figure content
- titles
- output directory

**Step 2: Run focused tests**

Run:
`pytest tests/unit/test_qc_batch_scaling.py -q`

Expected:
PASS

### Task 3: Run the focused regression suite

**Files:**
- No new source edits required if earlier tasks pass

**Step 1: Run the Step 3-focused suite**

Run:
`pytest tests/unit/test_qc_batch_scaling.py tests/unit/test_plotting.py tests/unit/test_qc_lowess.py -q`

Expected:
PASS

**Step 2: Fix only filename-related regressions if any appear**

- Do not modify Step 3 math or plotting logic
- Keep the scope to output filename expectations

**Step 3: Re-run the suite**

Run:
`pytest tests/unit/test_qc_batch_scaling.py tests/unit/test_plotting.py tests/unit/test_qc_lowess.py -q`

Expected:
PASS

### Task 4: Verify on the real workbook

**Files:**
- No source edits required if earlier tasks pass

**Step 1: Re-run Step 3 on the user workbook**

Use:
`C:\Users\user\Desktop\MS Data process package\ms-preprocessing-toolkit\OUTPUT\DNP\ISTD測試\STEP4_program2_DNA_alignment_test_ISTD.xlsx`

Expected:
- a fresh `QC_Batch_Scaling_plots/<session>/` directory is created
- it contains:
  - `Fig1_PCA_by_batch_<timestamp>.png`
  - `Fig2_PCA_by_sample_type_<timestamp>.png`
  - `Fig3_Residual_Analysis_<timestamp>.png`

**Step 2: Inspect the directory contents**

- Confirm no new `Fig4_` or `Fig6_` files are generated by the new Step 3 run

**Step 3: Report the latest verified plots directory**

- Provide the fresh output directory path and the three exact filenames
